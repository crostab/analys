const INTERSECT = -1;
const UNION = 0;
const LEFT = 1;
const RIGHT = 2;
const JoinModes = {
  intersect: INTERSECT,
  union: UNION,
  left: LEFT,
  right: RIGHT
};

export { INTERSECT, JoinModes, LEFT, RIGHT, UNION };
