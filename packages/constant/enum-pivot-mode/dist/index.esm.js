const MERGE = 'merge';
const ACCUM = 'accum';
const INCRE = 'incre';
const COUNT = 'count';
const AVERAGE = 'average';
const MAX = 'max';
const MIN = 'min';
const FIRST = 'first';
const LAST = 'last';
const PivotModes = {
  merge: MERGE,
  accum: ACCUM,
  incre: INCRE,
  count: COUNT,
  average: AVERAGE,
  max: MAX,
  min: MIN,
  first: FIRST,
  last: LAST
};

export { ACCUM, AVERAGE, COUNT, FIRST, INCRE, LAST, MAX, MERGE, MIN, PivotModes };
