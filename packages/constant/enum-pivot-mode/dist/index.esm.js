const MERGE = -1;
const ACCUM = 0;
const INCRE = 1;
const COUNT = 2;
const SUM = 1;
const PivotModes = {
  merge: MERGE,
  accum: ACCUM,
  incre: INCRE,
  count: COUNT,
  sum: SUM
};

export { ACCUM, COUNT, INCRE, MERGE, PivotModes, SUM };
