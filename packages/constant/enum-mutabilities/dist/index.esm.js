/** @type {{mutate: boolean}} */
const MUTABLE = {
  mutate: true
};
const MUT = MUTABLE;
/** @type {{mutate: boolean}} */

const IMMUTABLE = {
  mutate: false
};
const IMMUT = IMMUTABLE;

export { IMMUT, IMMUTABLE, MUT, MUTABLE };
